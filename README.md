# CULTURO BG

This repository contains the frontend for CULTURO BG (previously Subitiq VBG).

Branding changes made on 2026-03-28:

- Site name updated to **CULTURO BG**.
- New logo assets added under `frontend/src/assets/`.
- Routing migrated to `atomic-router` and state moved to `effector`.

Logo files:

- `frontend/src/assets/logo.svg` (primary, light-on-dark)
- `frontend/src/assets/logo-compact.svg` (compact)
- `frontend/src/assets/logo-dark.svg` (dark-on-light)

Next steps:

- Rename local folder to `CULTURO BG` (optional). Close VS Code before renaming.
- Rename remote repository on GitHub to `CULTURO-BG` and update remote URL locally.

Commands to update remote (example):

```bash
git remote set-url origin git@github.com:<user>/CULTURO-BG.git
```

